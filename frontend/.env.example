# Frontend Environment Template:
# Do not place actual secrets in this file. It is safe to commit.

# Replace the URL below with your backend URL when deploying to production (e.g. Vercel/Netlify).
NEXT_PUBLIC_API_URL=http://localhost:8000
